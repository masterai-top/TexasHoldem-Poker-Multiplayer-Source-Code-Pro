# 实时房间消息流程

客户端与房间消息文件是多人服务端的关键入口。每条消息都应经过身份、房间、牌局状态、行动顺序和参数范围校验。

## 建议测试

- 重复请求和消息重放
- 玩家不在房间时发送行动
- 非当前行动玩家提交操作
- 网络断线与重连后的状态恢复
- 超时与自动操作同时触发
- 房间广播范围和消息顺序
- 旧客户端协议兼容

账号令牌、私人消息和其他敏感数据不得写入普通日志。客户端计算出的牌局结果不能直接作为服务端结算依据。

